# 安全策略

本项目是个人设备上的**辅助浏览脚本**，不是绕过平台规则的工具。阅读本文件可以了解它
承诺做什么、明确不做什么，以及发现问题后如何报告。

## 支持范围

只处理以下四类淘宝“赚金币抵钱”内部的纯浏览任务，并在进入任务流程前顺带完成每日签到：

| 模块 | 标题结构 | 允许的动作 |
|---|---|---|
| 搜一搜 | `搜一搜…` | 选择“搜索发现”关键词，在结果页上下滑动 |
| 看看# | `看看#…` | 信息流停留与上滑 |
| 发现精选好物 | 精确标题 | 信息流停留与上滑 |
| 好物沉浸看 | 精确标题“好物沉浸看” | 信息流沉浸浏览（停留/上滑，动态分母） |

未出现在注册表中的任务不会被执行：默认扫描不为它们生成候选，直接调用时也先返回
`unsupported_task`，再谈任何设备动作。

每日签到只在淘金币根页安全点击“签到领金币”入口（或独立签到弹窗的领取按钮）一次，点击后重新读屏验证；它不是充值、交易、提现动作，不涉及金额或支付密码，失败也不阻塞任务流程。

## 明确不做的事

这些是设计约束，不是待办事项：

- 不绕过验证码、滑块、登录验证或任何风险控制；
- 不执行第三方应用任务（头条、支付宝、蚂蚁森林等）；
- 不执行交易、充值、邀请、助力、游戏、外卖、红包类任务；
- 不通过 OCR 坐标点击商品卡片、加购、购买或下单按钮；
- 不扩大标题白名单来“兼容”未验证的任务；
- 不承诺无人值守完成全部任务。

## 失败关闭（fail-closed）

任一项不成立就停止，不重试、不降级、不坐标兜底：

- 当前包名不是淘宝，或 OCR 发现风控词；
- 入口动作按钮不唯一，或入口二次校验失败；
- 离开已知页面时执行**有界回退**：返回任务列表最多 `MAX_BACKS=6` 次，整场收尾动作预算 `SETTLE_BUDGET_S=15` 秒；每一次点击/滑动/返回前都在最后一刻重新确认当前包名仍是淘宝。超过次数或预算、任一步发现不在淘宝包、或命中风控词，立即停止；
- 设备断连、OCR 不可用、总期限或单任务期限到期。

进入信息流后的浏览也只做停留与滑动。每轮浏览后会退出到淘金币页面完成结算，再重新
打开弹窗读取进度，不在弹窗内连续浏览。

面对未知/频道子页面，脚本采用上述有界回退**主动退回已知界面（执行完退出到初始界面）**，而不是停在未知页面原地不动；整个回退过程受“每步包名确认 + 次数上限 + 15 秒收尾预算”三重约束，绝不越界操作淘宝包以外的应用，也不点击回退路径上的任何页面控件。

## 报告问题

请通过 [GitHub Issues](https://github.com/Linshi7766/taojinbi-Mav/issues) 报告，并附：

- 复现步骤（是否处于 `--dry-run`）；
- 脱敏后的日志片段（`logs/` 中的 JSONL 事件，不要贴原始 OCR 文本或截图）；
- 期望行为与实际行为。

**不要提交**：真实设备序列号、局域网地址、本机绝对路径、账号信息、Cookie/Token，
以及包含上述内容的日志或截图。

## 隐私

- 运行日志只写结构化事件（`run_started` / `task_started` / `task_finished` /
  `run_finished`）与脱敏标签，不写任务标题原文，不写设备标识；
- 日志目录 `logs/` 已在 `.gitignore` 中排除，不会进入版本库；
- OCR 临时截图只写到工作区，不上传、不随仓库发布。
