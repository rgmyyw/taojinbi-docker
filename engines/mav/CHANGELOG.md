# 更新日志

本项目的 GitHub 公开源码仓库已上线；正式版本标签/发行包尚未发布。以下按时间倒序记录已经落地的变更，格式遵循 [Keep a Changelog](https://keepachangelog.com/)。

## [未发布] — 正式版本准备中

### 新增

- **徽标驱动浏览时长**：结果页顶部“浏览N秒可领”徽标（2026-08-29 实测要求在
  10/25/30 秒间变化）决定滑动时长——读到徽标持续滑动至其连续消失，读不到按
  约 30 秒保底，硬上限 80 秒；要求秒数打印留痕。修复平台上调时长后浏览不计数。
- **弹窗关闭控件**：结算回退到达淘金币页但任务弹窗仍开着（遮挡“赚更多金币”、
  回合计数不结算）时，点击右上角“更多”关闭控件（OCR 精确唯一匹配 + 坐标
  校验）并确认按钮恢复可见。
- **页面身份感知回退**：回退每步识别页面——淘金币页为根，被遮挡时原地失败
  绝不越界按返回（防过冲首页）；未知页面原地停止；徽标消失的结果页经
  `is_search_flow_page`（“搜索”按钮 + 排除首页）识别为流程页。
- **隐私安全诊断指纹**：策略失败（入口不可用/候选缺失等）时发 `page_diagnostic`
  事件，只含布尔/计数信号（span 数、各页面判定结果），不含任何 OCR 原文。
- **守候整场监控面板**：`wait_for_task.py` 全程发会话事件（含 30 秒心跳），
  `scripts/wait_panel.py` 独立窗口实时渲染状态/任务计数/休息倒计时，会话结束
  3 秒或 90 秒无心跳自动退出（`--no-panel` 可关）。
- `--task` 选择器：只执行指定注册任务类型（`search` / `hashtag` / `featured_goods`），
  未指定时扫描全部已注册任务（`e6d4613`）。
- 每次运行自动弹出终端进度面板（Python 进程，`--auto-exit` 在运行结束后自动关闭）；
  `--no-watch` 可关闭（`569377d`）。
- execute 启动时若不在任务弹窗，会自动在淘金币页面点击“赚更多金币”打开弹窗并校验列表
  锚点；入口 OCR 缺失时有界重试（总计最多读取 3 次）后仍失败即停止（`addd8cc`、
  `83b5eb1`）。
- 搜一搜结果页改为高频滑动（每约 2 秒一次，共 5 次），对齐手动浏览节奏
  （`5c6b3d7`）。

### 修复

- **搜一搜进度停滞（stalled）**：根因是每轮浏览停留在任务弹窗内、没有退出到淘金币页面
  结算。现在每轮浏览后会安全退出弹窗到“赚更多金币”界面完成结算，再重新打开弹窗读取
  进度（`93cd1db`）。真机验证进度由 `3/5` 推进到 `4/5` 并归零重置，用户已确认金币到账
  （`4f3a145`）。
- 每轮浏览只选择一个“搜索发现”关键词，不再一次浏览多个关键词（`78bd992`）。
- 退出路径改为 `_safe_back_to_coin_page()`：不依赖搜索结果页的 OCR 锚点即可安全返回，
  页面身份无法识别时也能按包名、风险词、目标锚点与有界次数兜底（`e356d71`）。
- 搜索结果页现在被视为任务流程内页面，允许按返回键退出（`4e4adac`）。
- `only_titles` 前缀可以匹配轮换标题，使 `--task search` 能定位 `搜一搜…`
  （`658b48a`）。
- 已浏览且返回后任务行消失、同时读到过进度时，分类为“很可能完成”而不是“未完成”
  （`bbcd6ab`）。
- 整次运行结束后退出到淘金币首页，便于刷新出后续任务（`d612e48`）。

### 安全

- 所有返回动作都保持有界与失败关闭：包名不是淘宝、出现风控词、OCR 不可用或次数耗尽时
  立即停止，不坐标兜底、不点击下一个任务。
- 精选好物真机证据已补齐：唯一 CLI + GPU + 单任务执行并经人工到账确认（`7196f06`）；
  安全返回设计保持有界与失败关闭，未因到账证据放宽。

### 文档

- README 按模块区分验证状态：`搜一搜`、`看看#`、`发现精选好物` 均已真机执行并确认金币到账
  （`6b1e5f3`、`7196f06`）。
- 新增 `SECURITY.md`、`CONTRIBUTING.md` 与 GitHub Actions 离线测试工作流（`bb267b3`）；
  `pyproject.toml` 显式声明直接依赖（`9f2db53`）。
- 公开导出清理：`进度跟踪/` 与 `docs/superpowers/plans/` 从索引移除（`2f9fb0e`），
  运行截图加入忽略（`74495c4`）。

### 工程

- 三模块真机证据补齐：`发现精选好物` 完成唯一 CLI + GPU + 单任务 + 人工到账确认，
  三模块全部到账（`7196f06`）。
- 进度归类保守化：固定分母不匹配（如 `2/3` 与 `2/5`）时归 `unfinished`/`partial`，
  不再误报“很可能完成”（`641f976`、`0dba4a7`）。
- README 唯一 CLI 合同：真机只能走 `scripts/run_taojinbi.py`，统一 `--gpu`，
  自动进度面板不得关闭或传 `--no-watch`，禁止直接 import `run_ocr_entry()`（`b393ed9`）。
- **src 布局重组**：共享组件迁入 `src/taojinbi_mav/`（`runtime/` 子包）、入口迁至
  `scripts/run_taojinbi.py`、watch 面板改为 `python -m taojinbi_mav.runtime.watch`；
  根目录保留轻量 shim 兼容旧脚本（`23b4153`）。
- **包名更名**：Python 包 `coin11_tb` → `taojinbi_mav`，仓库名 `coin11-tb` →
  `taojinbi-Mav`；`pyproject.toml`、CLI、watch 面板命令、CI 与文档全部同步
  （公开 Beta 发布前完成，无外部用户受影响）。

## 2026-08-27

- 真机验收：`搜一搜` 首次确认金币到账，随后多轮执行验证通过（`dac9daa` 等）。
- 新增运行时进度面板与任务事件日志（`f88101a`）。
- 整次运行结束后回到任务列表（`4487e37`）。
- 记录停滞轮次并暂停自动重试，改由人工确认后再继续（`7bfe95a`）。

## 2026-08-25 及更早

- 公开版运行入口加固：deadline、CLI、`--dry-run`、脱敏结构化日志、退出码与 `Ctrl+C`
  （Phase 18，Task 1–9）。
- 三模块注册表与任务策略抽取完成（Phase 17 及之前）。
