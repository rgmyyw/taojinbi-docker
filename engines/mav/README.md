# taojinbi-Mav

使用 `uiautomator2` 和 OCR 辅助完成淘宝任务。所有命令均从项目根目录运行：

> 淘金币核心源自 [czl0325/coin11-tb](https://github.com/czl0325/coin11-tb)
>（Apache-2.0）并在其基础上大幅重写。私有全量仓库仍保留历史兼容资产；
> 它们不属于公开四模块标准入口。

```powershell
Set-Location '<项目根目录>'
```

## 环境与测试

项目使用工作区内的 `.venv`。如果环境已经存在且依赖完整，不需要重复安装；
新检出或依赖缺失时，使用 pyproject 元数据进行 editable 安装：

```powershell
& .\.venv\Scripts\python.exe -m pip install -e '.[gpu]'
```

editable 安装提供 `taojinbi_mav` 包和 GPU 依赖；当前不提供 `taojinbi-mav` 全局命令，仍从项目根目录运行 `scripts/` 下的入口。

离线运行测试：

```powershell
& .\.venv\Scripts\python.exe -m unittest discover -s tests -t . -q
```

## 入口

- `scripts/run_taojinbi.py` 是唯一的真机自动化入口，仅处理下表注册的四种任务，并在进入任务流程前顺带安全完成每日签到。普通执行时若当前不在任务弹窗，会在淘金币页面自动点击“赚更多金币”打开弹窗并校验列表锚点；`--dry-run` 不会导航，仍需手动打开“赚金币抵钱”任务弹窗。
- `src/taojinbi_mav/task_strategies.py` 只负责已通过安全筛选后的任务交互策略；信息流任务只停留/上滑，“搜一搜”只点击搜索发现关键词并在结果页上下滑动。该模块不负责放行任务、进度完成判定或批次执行。
- GitHub 公开源码仓库已上线：[Linshi7766/taojinbi-Mav](https://github.com/Linshi7766/taojinbi-Mav)；正式版本标签/独立发行包尚未发布。

旧版 `淘宝多任务执行.py` 不属于当前 OCR 真机运行合同；不要运行 `淘宝多任务执行.py` 或其他入口替代标准 CLI。

从项目根目录启动唯一 CLI；把尖括号内容替换为真实设备序列号，按需选择任务和数量：

```powershell
& .\.venv\Scripts\python.exe .\scripts\run_taojinbi.py --serial <设备序列号> --task featured_goods --gpu --max-tasks 1
```

## 循环守候（推荐日常用法）

`scripts/wait_for_task.py` 在唯一 CLI 外层提供守候循环；每轮仍完整调用
`scripts/run_taojinbi.py`，守候层只负责调度、读取脱敏 JSONL 结果和按档位休息：

```powershell
& .\.venv\Scripts\python.exe .\scripts\wait_for_task.py --serial <设备序列号>
```

- 默认等待任意已注册任务，`--task` 可限制为一种任务；每轮最多执行一个任务。
- 无候选时随机等待 3–8 分钟；完成后休息 5–15 分钟；未完成时休息 1–3 分钟。
- 一个守候阶段默认最多检查 8 轮，整场默认 240 分钟；任何非零退出码都停止并等待人工核对。
- 整场面板和 OCR sidecar 默认自动启动；`--no-panel` / `--no-ocr-sidecar` 仅用于诊断。

允许的任务参数为 `--task search`、`--task hashtag`、`--task featured_goods` 或 `--task immersive`；省略 `--task` 时扫描全部已注册任务。真机运行统一显式传 `--gpu`，逐步扩大前再调整 `--max-tasks N`。

Windows 上通过该 CLI 正式运行时，会自动启动 `python -m taojinbi_mav.runtime.watch` 实时进度面板窗口。面板读取 `logs/` 中的 JSONL 事件日志，并在运行结束后自动关闭；这是预期行为。不要传 `--no-watch`，也不要主动关闭进度面板。

不要通过 `import` 直接调用 `run_ocr_entry()` 运行真机任务；这种方式不会启动标准进度面板，而且会绕过 CLI 的统一参数、日志和退出码行为。

默认最多执行 1 个安全浏览任务；传 `--max-tasks 0` 只检查入口不执行任务。设备序列号由 `--serial` 或环境变量 `TAOJINBI_DEVICE_SERIAL` 提供；**仓库不保存任何默认设备地址**，两者都缺失时以退出码 3 安全失败且不连接设备。CLI 仍支持 CPU 作为兼容选项，但本项目后续真机自动化统一使用 `--gpu`。未连接设备、账号处于风控冷却期或未完成人工确认时，不要启动真实任务。

## 安全范围

当前离线实现注册以下四个任务模块；此外会在淘金币根页自动完成每日签到：

| 模块 | 标题结构 | 当前执行策略 | 验证状态 |
|---|---|---|---|
| 搜一搜 | `搜一搜…` | 搜索发现关键词后仅上下滑动 | 真机执行通过，金币到账已确认 |
| 看看# | `看看#…` | 信息流停留与上滑 | 真机执行通过，金币到账已确认 |
| 发现精选好物 | 精确标题 | 信息流停留与上滑 | 真机执行通过，金币到账已确认 |
| 好物沉浸看 | 精确标题“好物沉浸看” | 信息流沉浸浏览（feed 停留/上滑），动态分母 | 真机执行通过，金币到账已确认 |

- 标准注册表的 OCR 发现和默认主入口的精确回定位都必须取得 profile；表外任务无法经注册表获得执行资格，即使标题或描述包含“浏览”也不例外。默认扫描不会为它们产生候选，会安全跳过并以空/安全汇总结束；只有直接调用 `run_one_safe_browse_task(未知标题)` 时，才会在任何设备动作前返回 `unsupported_task`。
- `搜一搜`、`看看#` 可以在 OCR 漏读时省略同行“浏览”证据；`发现精选好物` 必须取得同行“浏览”证据。`看看#` 与 `好物沉浸看` 启用动态分母（任务行分母随机轮换时按最新观测基线推进）；其中只有 `看看#` 启用轮换标题身份。
- 头条、支付宝、蚂蚁森林等第三方应用任务会跳过。
- 下单、购买、邀请、充值、助力、游戏、外卖、红包等交易或高风险特征会跳过；外部红包、抢红包和带交易描述的任务均默认跳过。
- 注册任务进入信息流后只停留和上滑；“搜一搜”随机选择“搜索发现”关键词后只在结果页上下滑动，绝不点击商品详情。所有流程均不点击加购、购买、下单或验证码控件；包名、OCR 证据或入口二次校验失败时安全退出。
- 如果任务已进入并浏览、返回后任务行暂时消失，OCR 入口会最多两次退出到淘金币页面，动态点击“赚更多金币”、等待列表并滚动一屏后复查；只有同一注册身份、分母未变化且进度未回落的未完成任务才自动续跑。分母变化或进度回落表示任务周期已经轮换，立即停止且不进入新周期；两次仍找不到也停止当前任务，不点击下一个任务。

四模块注册表、运行时加固、依赖元数据和干净公开导出均已完成；GitHub 公开源码仓库已上线。只读 dry-run 真机验收以及 `搜一搜`、`看看#`、`发现精选好物`、`好物沉浸看` 四个任务的真机执行和金币到账、淘金币根页每日签到（点击“签到领金币”入口）均已有记录；正式版本标签/独立发行包尚未发布。

## 公开版运行合同

本节说明公开 CLI 的运行前提、支持范围、时间预算、退出码、日志与隐私边界。只读 dry-run 真机验收已通过；真实执行仍受任务随机刷新、账号状态和风控影响，使用前必须人工确认当前页面与授权范围。

### 运行前提

- 普通执行：把手机停在**淘金币页面**（能看到“赚更多金币”）即可，脚本会自动打开“赚金币抵钱”任务弹窗；当前已在弹窗列表页时直接使用。脚本不会导航到淘金币页面本身，也不会在淘宝以外的页面操作。
- `--dry-run`：不会导航或打开弹窗，运行前必须**手动打开淘宝“赚金币抵钱”任务弹窗**并停留在列表页。
- `--dry-run` 是只读检查：只扫描当前可见一屏，不会点击、不会滑动、不会返回、不会领取奖励，也不会读取任务列表以外的内容。
- 未连接设备、账号处于风控冷却期或未完成人工确认时，不要启动真实任务。

### 支持模块（四项）与每日签到

| 模块 | 脱敏报告格式 | 执行策略 |
|---|---|---|
| 搜一搜 | `搜一搜…` | 随机选择“搜索发现”关键词后仅上下滑动，不点击商品 |
| 看看# | `看看#…` | 信息流停留与上滑 |
| 发现精选好物 | 精确标题 | 信息流停留与上滑 |
| 好物沉浸看 | 精确标题“好物沉浸看” | 信息流沉浸浏览，动态分母 |

### 每日签到（自动顺带完成）

- 普通执行进入任务流程前，若淘金币根页出现“签到领金币”入口，会用动作前最后一刻校验包名的安全点击点一下即完成签到（真机 2026-09-02 证实 +120 金币到账），点击后重新读屏验证入口消失，不假设成功；若遇到独立签到弹窗则只点其领取文案按钮。
- 签到**不是充值/交易/提现动作**，不涉及支付密码或金额；没有签到入口或签到失败时不阻塞后续任务流程。

### 已移除任务（不在支持列表）

拍立淘、酒店超抵、去省钱卡领红包、淘金币充话费 等任务已从支持列表移除。默认扫描不会为这些标题生成候选，会安全跳过并以空/安全汇总结束；只有直接调用 `run_one_safe_browse_task(未知标题)` 才会在设备动作前返回 `unsupported_task`。

### 时间预算与中止

- 默认时间预算：`--dry-run` 2 分钟、单任务 20 分钟、整次运行 30 分钟。
- 超时或首次 `Ctrl+C` 后，脚本会先尝试安全返回任务列表（最多 10 秒恢复宽限）再停止，不会自动开始下一个任务。
- 协作式时限：脚本只在动作与等待边界检查时间，不使用可能在后台继续动作的超时线程；因此不能保证硬中断单次卡死的 ADB/OCR 调用，只能通过动作边界检查尽快停止。
- `Ctrl+C` 两阶段退出：dry-run 中直接退出；execute 中第一次触发进入 10 秒恢复窗口，恢复期间再次触发立即退出且不再点击或滑动。

### 退出码

`0` 正常完成 / dry-run 成功 / 无候选；`1` 已执行但未确认完成；`2` 参数错误；`3` 设备/OCR/入口检查失败；`4` 安全边界停止；`5` 超时；`130` 用户 `Ctrl+C` 中止。

### 日志与隐私边界

- 控制台输出可读中文进度；同时在 `logs/` 写入 UTF-8 JSON Lines 事件日志。
- 日志不记录 OCR 原始截图、设备序列号或商品名称；任务统一使用脱敏标签（如 `看看#…`、`搜一搜…`）。

### 离线测试与只读 dry-run

离线运行测试：

```powershell
& .\.venv\Scripts\python.exe -m unittest discover -s tests -t . -q
```

只读 dry-run（需要复核时，在用户授权后手动打开弹窗再执行）：

```powershell
& .\.venv\Scripts\python.exe ".\scripts\run_taojinbi.py" --dry-run
```

dry-run 命令会连接 ADB 并 OCR 读屏，但**不会产生任何 UI 动作**；它只验证当前可见一屏的识别、脱敏报告、退出码与零动作。普通 execute 真机测试不属于本阶段验收步骤。

## 免责声明

- 本项目为**非官方**个人工具，与淘宝、阿里巴巴集团无任何关联或背书。
- 仅供个人学习研究使用；使用本项目产生的一切后果由使用者自行承担，请遵守平台规则与当地法律法规。
- 项目不提供任何明示或暗示的担保。

## 点赞历史

[![Star History Chart](https://api.star-history.com/svg?repos=Linshi7766/taojinbi-Mav&type=Date)](https://star-history.com/#Linshi7766/taojinbi-Mav&type=Date)
<br><br>
